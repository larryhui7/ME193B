[t_sol, x_sol, t_I] = simulate_two_link_walker(10);

theta = x_sol(:,1);
phi = x_sol(:,2) - x_sol(:,1);
figure;
plot(t_sol, theta); hold on;
plot(t_sol, phi);
xlabel('Time (s)');
ylabel("Angle ");
title(['Two-Link Walker Angles over Time for N = ' num2str(N)]);
legend('\theta(t)', "\phi(t)");
grid on;
hold off;
animate_two_link_walker(t_sol, x_sol, 0.01, t_I)